# Add these only after the website is deployed against native PostgreSQL.
DISCORD_OUTBOX_URL=https://101stdoombattalion.com/api/internal/discord-outbox
ATTENDANCE_API_URL=https://101stdoombattalion.com/api/internal/discord-attendance
DISCORD_OUTBOX_POLL_INTERVAL_MS=10000

# WEBSITE_BOT_SECRET must be at least 32 characters and match the website's
# server-only value. Rotate both copies together before enabling these URLs.
# Do not commit or paste its value.
